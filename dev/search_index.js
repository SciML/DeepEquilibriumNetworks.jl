var documenterSearchIndex = {"docs":
[{"category":"section","location":"references/#References","page":"References","text":"Bai, S.; Kolter, J. Z. and Koltun, V. (2019). Deep Equilibrium Models, arXiv:1909.01377 [cs, stat]. Accessed on Sep 13, 2021, arXiv: 1909.01377.\n\n\n\nBai, S.; Koltun, V. and Kolter, J. Z. (2020). Multiscale Deep Equilibrium Models, arXiv:2006.08656 [cs, stat]. Accessed on Sep 14, 2021, arXiv: 2006.08656.\n\n\n\nPal, A.; Edelman, A. and Rackauckas, C. (2022). Mixing implicit and explicit deep learning with skip DEQs and infinite time neural odes (continuous DEQs). Training 4, 5.\n\n\n\n","title":"References"},{"category":"section","location":"api/#Deep-Equilibrium-Models","page":"API References","text":"(Bai et al., 2019) introduced Discrete Deep Equilibrium Models which drives a Discrete Dynamical System to its steady-state. (Pal et al., 2022) extends this framework to Continuous Dynamical Systems which converge to the steady-stable in a more stable fashion. For a detailed discussion refer to (Pal et al., 2022).\n\nTo construct a continuous DEQ, any ODE solver compatible with DifferentialEquations.jl API can be passed as the solver. To construct a discrete DEQ, any root finding algorithm compatible with NonlinearSolve.jl API can be passed as the solver.","title":"Deep Equilibrium Models"},{"category":"section","location":"api/#Choosing-a-Solver","page":"API References","text":"","title":"Choosing a Solver"},{"category":"section","location":"api/#Root-Finding-Algorithms","page":"API References","text":"Using Root Finding Algorithms give fast convergence when possible, but these methods also tend to be unstable. If you must use a root finding algorithm, we recommend using:\n\nNewtonRaphson or TrustRegion for small models\nLimitedMemoryBroyden for large Deep Learning applications (with well-conditioned Jacobians)\nNewtonRaphson(; linsolve = KrylovJL_GMRES()) for cases when Broyden methods fail\n\nNote that Krylov Methods rely on efficient VJPs which are not available for all Lux models. If you think this is causing a performance regression, please open an issue in Lux.jl.","title":"Root Finding Algorithms"},{"category":"section","location":"api/#ODE-Solvers","page":"API References","text":"Using ODE Solvers give slower convergence, but are more stable. We generally recommend these methods over root finding algorithms. If you use implicit ODE solvers, remember to use Krylov linear solvers, see OrdinaryDiffEq.jl documentation for these. For most cases, we recommend:\n\nVCAB3() for high tolerance problems\nTsit5() for high tolerance problems where VCAB3() fails\nIn all other cases, follow the recommendation given in OrdinaryDiffEq.jl documentation","title":"ODE Solvers"},{"category":"section","location":"api/#Sensitivity-Analysis","page":"API References","text":"For MultiScaleNeuralODE, we default to GaussAdjoint(; autojacvec = ZygoteVJP()). A faster alternative would be BacksolveAdjoint(; autojacvec = ZygoteVJP()) but there are stability concerns for using that. Follow the recommendation given in SciMLSensitivity.jl documentation.\nFor Steady State Problems, we default to SteadyStateAdjoint(; linsolve = SimpleGMRES(; blocksize, linsolve_kwargs = (; maxiters=10, abstol=1e-3, reltol=1e-3))). This default will perform poorly on small models. It is recommended to pass sensealg = SteadyStateAdjoint() or sensealg = SteadyStateAdjoint(; linsolve = LUFactorization()) for small models.","title":"Sensitivity Analysis"},{"category":"section","location":"api/#Standard-Models","page":"API References","text":"DEQs is exported as an alias for the DeepEquilibriumNetworks module.","title":"Standard Models"},{"category":"section","location":"api/#MultiScale-Models","page":"API References","text":"","title":"MultiScale Models"},{"category":"section","location":"api/#Solution","page":"API References","text":"","title":"Solution"},{"category":"type","location":"api/#DeepEquilibriumNetworks.DeepEquilibriumNetwork","page":"API References","text":"DeepEquilibriumNetwork(model, solver; init = missing, jacobian_regularization = nothing,\n    problem_type::Type = SteadyStateProblem{false}, kwargs...)\n\nDeep Equilibrium Network as proposed in (Bai et al., 2019) and (Pal et al., 2022).\n\nArguments\n\nmodel: Lux layer defining the equilibrium map.\nsolver: Solver for the equilibrium problem. ODE solvers and nonlinear solvers are both supported.\n\nKeywords\n\ninit: Initial condition layer for the equilibrium problem. If nothing, the initial condition is set to zero(x). If missing, it is set to WrappedFunction(zero). Otherwise, pass a Lux layer called as init(x, ps, st).\njacobian_regularization: Jacobian stabilization backend. Supported values are nothing, AutoForwardDiff, AutoFiniteDiff, and AutoZygote.\nproblem_type: Equilibrium problem type. Use ODEProblem to construct an ODE-based network; defaults to SteadyStateProblem.\nkwargs: Additional keyword arguments passed to SciMLBase.solve.\n\nReturns\n\nReturns a Lux layer. Calling the layer returns the model output and a state whose solution field contains a DeepEquilibriumSolution.\n\nExample\n\njulia> using DeepEquilibriumNetworks, Lux, SteadyStateDiffEq, Random\n\njulia> model = DeepEquilibriumNetwork(\n           Parallel(+, Dense(2, 2; use_bias=false), Dense(2, 2; use_bias=false)),\n           SSRootfind(); verbose=false);\n\njulia> rng = Xoshiro(0);\n\njulia> ps, st = Lux.setup(rng, model);\n\njulia> size(first(model(ones(Float32, 2, 1), ps, st)))\n(2, 1)\n\nSee also: SkipDeepEquilibriumNetwork, MultiScaleDeepEquilibriumNetwork, MultiScaleSkipDeepEquilibriumNetwork.\n\n\n\n\n\n","title":"DeepEquilibriumNetworks.DeepEquilibriumNetwork"},{"category":"function","location":"api/#DeepEquilibriumNetworks.SkipDeepEquilibriumNetwork","page":"API References","text":"SkipDeepEquilibriumNetwork(model, [init=nothing,] solver; kwargs...)\n\nSkip Deep Equilibrium Network as proposed in (Pal et al., 2022).\n\nThis is a convenience constructor for DeepEquilibriumNetwork that forwards init through the init keyword argument. If init is omitted, the initial condition is nothing.\n\nArguments\n\nmodel: Lux layer defining the equilibrium map.\ninit: Optional Lux layer used to construct the initial condition.\nsolver: Solver for the equilibrium problem.\n\nReturns\n\nReturns a DeepEquilibriumNetwork.\n\nExample\n\njulia> using DeepEquilibriumNetworks, Lux, SteadyStateDiffEq, Random\n\njulia> model = SkipDeepEquilibriumNetwork(\n           Parallel(+, Dense(2, 2; use_bias=false), Dense(2, 2; use_bias=false)),\n           SSRootfind(); verbose=false);\n\njulia> ps, st = Lux.setup(Xoshiro(0), model);\n\njulia> size(first(model(ones(Float32, 2, 1), ps, st)))\n(2, 1)\n\n\n\n\n\n","title":"DeepEquilibriumNetworks.SkipDeepEquilibriumNetwork"},{"category":"function","location":"api/#DeepEquilibriumNetworks.MultiScaleDeepEquilibriumNetwork","page":"API References","text":"MultiScaleDeepEquilibriumNetwork(main_layers::Tuple, mapping_layers::Matrix,\n    post_fuse_layer::Union{Nothing, Tuple}, solver,\n    scales::NTuple{N, NTuple{L, Int64}}; kwargs...)\n\nMulti Scale Deep Equilibrium Network as proposed in (Bai et al., 2020).\n\nArguments\n\nmain_layers: Tuple of Neural Networks. Each Neural Network is applied to the corresponding scale.\nmapping_layers: Matrix of Neural Networks. Each Neural Network is applied to the corresponding scale and the corresponding layer.\npost_fuse_layer: Neural Network applied to the fused output of the main layers.\nsolver: Solver for the rootfinding problem. ODE Solvers and Nonlinear Solvers are both supported.\nscales: Scales of the Multi Scale DEQ. Each scale is a tuple of integers. The length of the tuple is the number of layers in the corresponding main layer.\n\nFor keyword arguments, see DeepEquilibriumNetwork.\n\nExample\n\njulia> main_layers = (\n           Parallel(+, Dense(4 => 4, tanh; use_bias=false), Dense(4 => 4, tanh; use_bias=false)),\n           Dense(3 => 3, tanh), Dense(2 => 2, tanh), Dense(1 => 1, tanh));\n\njulia> mapping_layers = [NoOpLayer() Dense(4 => 3, tanh) Dense(4 => 2, tanh) Dense(4 => 1, tanh);\n                         Dense(3 => 4, tanh) NoOpLayer() Dense(3 => 2, tanh) Dense(3 => 1, tanh);\n                         Dense(2 => 4, tanh) Dense(2 => 3, tanh) NoOpLayer() Dense(2 => 1, tanh);\n                         Dense(1 => 4, tanh) Dense(1 => 3, tanh) Dense(1 => 2, tanh) NoOpLayer()];\n\njulia> model = MultiScaleDeepEquilibriumNetwork(\n           main_layers, mapping_layers, nothing, NewtonRaphson(), ((4,), (3,), (2,), (1,)));\n\njulia> rng = Xoshiro(0);\n\njulia> ps, st = Lux.setup(rng, model);\n\njulia> x = rand(rng, Float32, 4, 12);\n\njulia> size.(first(model(x, ps, st)))\n((4, 12), (3, 12), (2, 12), (1, 12))\n\n\n\n\n\n","title":"DeepEquilibriumNetworks.MultiScaleDeepEquilibriumNetwork"},{"category":"function","location":"api/#DeepEquilibriumNetworks.MultiScaleSkipDeepEquilibriumNetwork","page":"API References","text":"MultiScaleSkipDeepEquilibriumNetwork(main_layers::Tuple, mapping_layers::Matrix,\n    post_fuse_layer::Union{Nothing, Tuple}, [init = nothing,] solver,\n    scales::NTuple{N, NTuple{L, Int64}}; kwargs...)\n\nSkip Multi Scale Deep Equilibrium Network as proposed in (Pal et al., 2022). Alias which creates a MultiScaleDeepEquilibriumNetwork with init kwarg set to passed value.\n\nIf init is not passed, it creates a MultiScale Regularized Deep Equilibrium Network.\n\nArguments\n\nmain_layers: Tuple of Lux layers, one per scale.\nmapping_layers: Matrix of Lux layers mapping between scales.\npost_fuse_layer: Optional tuple of Lux layers applied after scale fusion.\ninit: Optional tuple of Lux layers used to construct the initial conditions.\nsolver: Solver for the equilibrium problem.\nscales: Output shape for each scale.\n\nReturns\n\nReturns a MultiScaleDeepEquilibriumNetwork.\n\n\n\n\n\n","title":"DeepEquilibriumNetworks.MultiScaleSkipDeepEquilibriumNetwork"},{"category":"function","location":"api/#DeepEquilibriumNetworks.MultiScaleNeuralODE","page":"API References","text":"MultiScaleNeuralODE(args...; kwargs...)\n\nConstruct a multi-scale neural ODE with the same arguments as MultiScaleDeepEquilibriumNetwork.\n\nThis forwards all positional and keyword arguments to MultiScaleDeepEquilibriumNetwork, while setting problem_type to ODEProblem{false}.\n\nReturns\n\nReturns a MultiScaleDeepEquilibriumNetwork configured with ODE dynamics.\n\n\n\n\n\n","title":"DeepEquilibriumNetworks.MultiScaleNeuralODE"},{"category":"type","location":"api/#DeepEquilibriumNetworks.DeepEquilibriumSolution","page":"API References","text":"DeepEquilibriumSolution(z_star, u0, residual, jacobian_loss, nfe, original)\n\nStores the solution data produced by a DeepEquilibriumNetwork and its variants.\n\nFields\n\nz_star: Steady state, or the final iterate reached when the solver stops.\nu0: Initial condition used by the equilibrium solve.\nresidual: Difference between z^* and f(z^* x).\njacobian_loss: Jacobian stabilization loss.\nnfe: Number of function evaluations.\noriginal: Original solver solution object.\n\n\n\n\n\n","title":"DeepEquilibriumNetworks.DeepEquilibriumSolution"},{"category":"section","location":"tutorials/reduced_dim_deq/#Modelling-Equilibrium-Models-with-Reduced-State-Size","page":"Modelling Equilibrium Models with Reduced State Size","text":"Sometimes we want don't want to solve a root finding problem with the full state size. This will often be faster, since the size of the root finding problem is reduced. We will use the same MNIST example as before, but this time we will use a reduced state size.\n\nusing DeepEquilibriumNetworks, SciMLSensitivity, Lux, NonlinearSolve, OrdinaryDiffEq,\n      Random, Optimisers, Zygote, LinearSolve, Dates, Printf, Setfield, OneHotArrays\nusing MLDatasets: MNIST\nusing MLUtils: DataLoader, splitobs\nusing LuxCUDA # For NVIDIA GPU support\n\nCUDA.allowscalar(false)\nENV[\"DATADEPS_ALWAYS_ACCEPT\"] = true\n\nconst cdev = cpu_device()\nconst gdev = gpu_device()\n\nfunction loadmnist(batchsize, train_split)\n    N = 2500\n    dataset = MNIST(; split=:train)\n    imgs = dataset.features[:, :, 1:N]\n    labels_raw = dataset.targets[1:N]\n\n    # Process images into (H,W,C,BS) batches\n    x_data = Float32.(reshape(imgs, size(imgs, 1), size(imgs, 2), 1, size(imgs, 3)))\n    y_data = onehotbatch(labels_raw, 0:9)\n    (x_train, y_train), (x_test, y_test) = splitobs((x_data, y_data); at=train_split)\n\n    return (\n        # Use DataLoader to automatically minibatch and shuffle the data\n        DataLoader(collect.((x_train, y_train)); batchsize, shuffle=true),\n        # Don't shuffle the test data\n        DataLoader(collect.((x_test, y_test)); batchsize, shuffle=false))\nend\n\nNow we will define the construct model function. Here we will use Dense Layers and downsample the features using the init kwarg.\n\nfunction construct_model(solver; model_type::Symbol=:regdeq)\n    down = Chain(FlattenLayer(), Dense(784 => 512, gelu))\n\n    # The input layer of the DEQ\n    deq_model = Chain(\n        Parallel(+,\n            Dense(128 => 64, tanh; use_bias=false, init_weight=truncated_normal(;\n                std=0.01)),   # Reduced dim of `128`\n            Dense(512 => 64, tanh; use_bias=false, init_weight=truncated_normal(;\n                std=0.01))),  # Original dim of `512`\n        Dense(64 => 64, tanh; use_bias=false, init_weight=truncated_normal(; std=0.01)),\n        Dense(64 => 128; use_bias=false, init_weight=truncated_normal(; std=0.01)))       # Return the reduced dim of `128`\n\n    if model_type === :skipdeq\n        init = Dense(512 => 128, tanh; use_bias=false, init_weight=truncated_normal(;\n            std=0.01))\n    elseif model_type === :regdeq\n        error(\":regdeq is not supported for reduced dim models\")\n    else\n        # This should preferably done via `ChainRulesCore.@ignore_derivatives`. But here\n        # we are only using Zygote so this is fine.\n        init = WrappedFunction(x -> Zygote.@ignore(fill!(similar(x, 128, size(x, 2)), false)))\n    end\n\n    deq = DeepEquilibriumNetwork(deq_model, solver; init, verbose=false,\n        linsolve_kwargs=(; maxiters=10), maxiters=10)\n\n    classifier = Chain(Dense(128 => 128, gelu), Dense(128, 10))\n\n    model = Chain(; down, deq, classifier)\n\n    # For NVIDIA GPUs this directly generates the parameters on the GPU\n    rng = Random.default_rng() |> gdev\n    ps, st = Lux.setup(rng, model)\n\n    # Warmup the forward and backward passes\n    x = randn(rng, Float32, 28, 28, 1, 2)\n    y = onehotbatch(rand(Random.default_rng(), 0:9, 2), 0:9) |> gdev\n\n    @printf \"[%s] warming up forward pass\\n\" string(now())\n    loss_function(model, ps, st, (x, y))\n    @printf \"[%s] warming up backward pass\\n\" string(now())\n    Zygote.gradient(first ∘ loss_function, model, ps, st, (x, y))\n    @printf \"[%s] warmup complete\\n\" string(now())\n\n    return model, ps, st\nend\n\nDefine some helper functions to train the model.\n\nconst logit_cross_entropy = CrossEntropyLoss(; logits=Val(true))\nconst mse_loss = MSELoss()\n\nfunction loss_function(model, ps, st, (x, y))\n    ŷ, st = model(x, ps, st)\n    l1 = logit_cross_entropy(ŷ, y)\n    l2 = mse_loss(st.deq.solution.z_star, st.deq.solution.u0) # Add in some regularization\n    return l1 + eltype(l2)(0.01) * l2, st, (;)\nend\n\nfunction accuracy(model, ps, st, dataloader)\n    total_correct, total = 0, 0\n    st = Lux.testmode(st)\n    for (x, y) in dataloader\n        target_class = onecold(y)\n        predicted_class = onecold(first(model(x, ps, st)))\n        total_correct += sum(target_class .== predicted_class)\n        total += length(target_class)\n    end\n    return total_correct / total\nend\n\nfunction train_model(solver, model_type)\n    model, ps, st = construct_model(solver; model_type)\n\n    train_dataloader, test_dataloader = loadmnist(32, 0.8) |> gdev\n\n    tstate = Training.TrainState(model, ps, st, Adam(0.0005))\n\n    @printf \"[%s] Training Model: %s with Solver: %s\\n\" string(now()) model_type nameof(typeof(solver))\n\n    @printf \"[%s] Pretrain with unrolling to a depth of 5\\n\" string(now())\n    @set! tstate.states = Lux.update_state(tstate.states, :fixed_depth, Val(5))\n\n    for _ in 1:2, (i, (x, y)) in enumerate(train_dataloader)\n\n        _, loss,\n        _, tstate = Training.single_train_step!(AutoZygote(), loss_function, (x, y), tstate)\n        if i % 10 == 1\n            @printf \"[%s] Pretraining Batch: [%4d/%4d] Loss: %.5f\\n\" string(now()) i length(train_dataloader) loss\n        end\n    end\n\n    acc = accuracy(model, tstate.parameters, tstate.states, test_dataloader) * 100\n    @printf \"[%s] Pretraining complete. Accuracy: %.5f%%\\n\" string(now()) acc\n\n    @set! tstate.states = Lux.update_state(tstate.states, :fixed_depth, Val(0))\n\n    for epoch in 1:3\n        for (i, (x, y)) in enumerate(train_dataloader)\n            _, loss,\n            _,\n            tstate = Training.single_train_step!(AutoZygote(), loss_function, (x, y), tstate)\n            if i % 10 == 1\n                @printf \"[%s] Epoch: [%d/%d] Batch: [%4d/%4d] Loss: %.5f\\n\" string(now()) epoch 3 i length(train_dataloader) loss\n            end\n        end\n\n        acc = accuracy(model, tstate.parameters, tstate.states, test_dataloader) * 100\n        @printf \"[%s] Epoch: [%d/%d] Accuracy: %.5f%%\\n\" string(now()) epoch 3 acc\n    end\n\n    @printf \"[%s] Training complete.\\n\" string(now())\n\n    return model, ps, tstate.states\nend\n\nNow we can train our model. We can't use :regdeq here currently, but we will support this in the future.\n\ntrain_model(NewtonRaphson(; linsolve=KrylovJL_GMRES()), :skipdeq)\nnothing # hide\n\ntrain_model(NewtonRaphson(; linsolve=KrylovJL_GMRES()), :deq)\nnothing # hide","title":"Modelling Equilibrium Models with Reduced State Size"},{"category":"section","location":"tutorials/basic_mnist_deq/#Training-a-Simple-MNIST-Classifier-using-Deep-Equilibrium-Models","page":"Training a Simple MNIST Classifier using Deep Equilibrium Models","text":"We will train a simple Deep Equilibrium Model on MNIST. First we load a few packages.\n\nusing DeepEquilibriumNetworks, SciMLSensitivity, Lux, NonlinearSolve, OrdinaryDiffEq,\n      Random, Optimisers, Zygote, LinearSolve, Dates, Printf, Setfield, OneHotArrays\nusing OrdinaryDiffEqAdamsBashforthMoulton: VCAB3\nusing MLDatasets: MNIST\nusing MLUtils: DataLoader, splitobs\nusing LuxCUDA # For NVIDIA GPU support\n\nCUDA.allowscalar(false)\nENV[\"DATADEPS_ALWAYS_ACCEPT\"] = true\n\nSetup device functions from Lux. See GPU Management for more details.\n\nconst cdev = cpu_device()\nconst gdev = gpu_device()\n\nWe can now construct our dataloader. We are using only limited part of the data for demonstration.\n\nfunction loadmnist(batchsize, train_split)\n    N = 2500\n    dataset = MNIST(; split=:train)\n    imgs = dataset.features[:, :, 1:N]\n    labels_raw = dataset.targets[1:N]\n\n    # Process images into (H,W,C,BS) batches\n    x_data = Float32.(reshape(imgs, size(imgs, 1), size(imgs, 2), 1, size(imgs, 3)))\n    y_data = onehotbatch(labels_raw, 0:9)\n    (x_train, y_train), (x_test, y_test) = splitobs((x_data, y_data); at=train_split)\n\n    return (\n        # Use DataLoader to automatically minibatch and shuffle the data\n        DataLoader(collect.((x_train, y_train)); batchsize, shuffle=true),\n        # Don't shuffle the test data\n        DataLoader(collect.((x_test, y_test)); batchsize, shuffle=false))\nend\n\nConstruct the Lux Neural Network containing a DEQ layer.\n\nfunction construct_model(solver; model_type::Symbol=:deq)\n    down = Chain(\n        Conv((3, 3), 1 => 64, gelu; stride=1), GroupNorm(64, 64), Conv((4, 4), 64 => 64; stride=2, pad=1))\n\n    # The input layer of the DEQ\n    deq_model = Chain(\n        Parallel(+,\n            Conv((3, 3), 64 => 64, tanh; stride=1, pad=SamePad(),\n                init_weight=truncated_normal(; std=0.01), use_bias=false),\n            Conv((3, 3), 64 => 64, tanh; stride=1, pad=SamePad(),\n                init_weight=truncated_normal(; std=0.01), use_bias=false)),\n        Conv((3, 3), 64 => 64, tanh; stride=1, pad=SamePad(),\n            init_weight=truncated_normal(; std=0.01), use_bias=false))\n\n    if model_type === :skipdeq\n        init = Conv((3, 3), 64 => 64, gelu; stride=1, pad=SamePad())\n    elseif model_type === :regdeq\n        init = nothing\n    else\n        init = missing\n    end\n\n    deq = DeepEquilibriumNetwork(deq_model, solver; init, verbose=false,\n        linsolve_kwargs=(; maxiters=10), maxiters=10)\n\n    classifier = Chain(GroupNorm(64, 64, relu), GlobalMeanPool(), FlattenLayer(), Dense(64, 10))\n\n    model = Chain(; down, deq, classifier)\n\n    # For NVIDIA GPUs this directly generates the parameters on the GPU\n    rng = Random.default_rng() |> gdev\n    ps, st = Lux.setup(rng, model)\n\n    # Warmup the forward and backward passes\n    x = randn(rng, Float32, 28, 28, 1, 2)\n    y = onehotbatch(rand(Random.default_rng(), 0:9, 2), 0:9) |> gdev\n\n    @printf \"[%s] warming up forward pass\\n\" string(now())\n    loss_function(model, ps, st, (x, y))\n    @printf \"[%s] warming up backward pass\\n\" string(now())\n    Zygote.gradient(first ∘ loss_function, model, ps, st, (x, y))\n    @printf \"[%s] warmup complete\\n\" string(now())\n\n    return model, ps, st\nend\n\nDefine some helper functions to train the model.\n\nconst logit_cross_entropy = CrossEntropyLoss(; logits=Val(true))\nconst mse_loss = MSELoss()\n\nfunction loss_function(model, ps, st, (x, y))\n    ŷ, st = model(x, ps, st)\n    l1 = logit_cross_entropy(ŷ, y)\n    l2 = mse_loss(st.deq.solution.z_star, st.deq.solution.u0) # Add in some regularization\n    return l1 + eltype(l2)(0.01) * l2, st, (;)\nend\n\nfunction accuracy(model, ps, st, dataloader)\n    total_correct, total = 0, 0\n    st = Lux.testmode(st)\n    for (x, y) in dataloader\n        target_class = onecold(y)\n        predicted_class = onecold(first(model(x, ps, st)))\n        total_correct += sum(target_class .== predicted_class)\n        total += length(target_class)\n    end\n    return total_correct / total\nend\n\nfunction train_model(solver, model_type)\n    model, ps, st = construct_model(solver; model_type)\n\n    train_dataloader, test_dataloader = loadmnist(32, 0.8) |> gdev\n\n    tstate = Training.TrainState(model, ps, st, Adam(0.0005))\n\n    @printf \"[%s] Training Model: %s with Solver: %s\\n\" string(now()) model_type nameof(typeof(solver))\n\n    @printf \"[%s] Pretrain with unrolling to a depth of 5\\n\" string(now())\n    @set! tstate.states = Lux.update_state(tstate.states, :fixed_depth, Val(5))\n\n    for _ in 1:2, (i, (x, y)) in enumerate(train_dataloader)\n\n        _, loss,\n        _, tstate = Training.single_train_step!(AutoZygote(), loss_function, (x, y), tstate)\n        if i % 10 == 1\n            @printf \"[%s] Pretraining Batch: [%4d/%4d] Loss: %.5f\\n\" string(now()) i length(train_dataloader) loss\n        end\n    end\n\n    acc = accuracy(model, tstate.parameters, tstate.states, test_dataloader) * 100\n    @printf \"[%s] Pretraining complete. Accuracy: %.5f%%\\n\" string(now()) acc\n\n    @set! tstate.states = Lux.update_state(tstate.states, :fixed_depth, Val(0))\n\n    for epoch in 1:3\n        for (i, (x, y)) in enumerate(train_dataloader)\n            _, loss,\n            _,\n            tstate = Training.single_train_step!(AutoZygote(), loss_function, (x, y), tstate)\n            if i % 10 == 1\n                @printf \"[%s] Epoch: [%d/%d] Batch: [%4d/%4d] Loss: %.5f\\n\" string(now()) epoch 3 i length(train_dataloader) loss\n            end\n        end\n\n        acc = accuracy(model, tstate.parameters, tstate.states, test_dataloader) * 100\n        @printf \"[%s] Epoch: [%d/%d] Accuracy: %.5f%%\\n\" string(now()) epoch 3 acc\n    end\n\n    @printf \"[%s] Training complete.\\n\" string(now())\n\n    return model, ps, tstate.states\nend\n\nNow we can train our model. First we will train a Discrete DEQ, which effectively means pass in a root finding algorithm. Typically most packages lack good nonlinear solvers, and end up using solvers like Broyden, but we can simply slap in any of the fancy solvers from NonlinearSolve.jl. Here we will use Newton-Krylov Method:\n\ntrain_model(NewtonRaphson(; linsolve=KrylovJL_GMRES()), :regdeq);\nnothing # hide\n\nWe can also train a continuous DEQ by passing in an ODE solver. Here we will use VCAB3() which tend to be quite fast for continuous Neural Network problems.\n\ntrain_model(VCAB3(), :deq);\nnothing # hide\n\nThis code is setup to allow playing around with different DEQ models. Try modifying the model_type argument to train_model to :skipdeq or :deq to see how the model behaves. You can also try different solvers from NonlinearSolve.jl and OrdinaryDiffEq.jl! Even 3rd party solvers from Sundials.jl will work, just remember to use CPU for those.","title":"Training a Simple MNIST Classifier using Deep Equilibrium Models"},{"category":"section","location":"#DeepEquilibriumNetworks.jl","page":"Home","text":"DeepEquilibriumNetworks.jl is a framework built on top of DifferentialEquations.jl and Lux.jl, enabling the efficient training and inference for Deep Equilibrium Networks (Infinitely Deep Neural Networks).","title":"DeepEquilibriumNetworks.jl"},{"category":"section","location":"#Installation","page":"Home","text":"To install DeepEquilibriumNetworks.jl, use the Julia package manager:\n\nusing Pkg\nPkg.add(\"DeepEquilibriumNetworks\")","title":"Installation"},{"category":"section","location":"#Quick-start","page":"Home","text":"using DeepEquilibriumNetworks, Lux, Random, NonlinearSolve, Zygote, SciMLSensitivity\nusing LuxCUDA  # For NVIDIA GPU support\n\nseed = 0\nrng = Random.default_rng()\nRandom.seed!(rng, seed)\n\nmodel = Chain(Dense(2 => 2),\n    DeepEquilibriumNetwork(\n        Parallel(+, Dense(2 => 2; use_bias=false), Dense(2 => 2; use_bias=false)),\n        NewtonRaphson()))\n\ngdev = gpu_device()\ncdev = cpu_device()\n\nps, st = Lux.setup(rng, model) |> gdev\nx = rand(rng, Float32, 2, 3) |> gdev\ny = rand(rng, Float32, 2, 3) |> gdev\n\nres, st_ = model(x, ps, st)\nst_.layer_2.solution\n\ngs = only(Zygote.gradient(p -> sum(abs2, first(model(x, p, st)) .- y), ps))","title":"Quick-start"},{"category":"section","location":"#Citation","page":"Home","text":"If you are using this project for research or other academic purposes, consider citing our paper:\n\n@article{pal2022continuous,\n  title={Continuous Deep Equilibrium Models: Training Neural ODEs Faster by Integrating Them to Infinity},\n  author={Pal, Avik and Edelman, Alan and Rackauckas, Christopher},\n  booktitle={2023 IEEE High Performance Extreme Computing Conference (HPEC)}, \n  year={2023}\n}\n\nFor specific algorithms, check the respective documentations and cite the corresponding papers.","title":"Citation"},{"category":"section","location":"#Contributing","page":"Home","text":"Please refer to the SciML ColPrac: Contributor's Guide on Collaborative Practices for Community Packages for guidance on PRs, issues, and other matters relating to contributing to SciML.\nSee the SciML Style Guide for common coding practices and other style decisions.\nThere are a few community forums:\nThe #diffeq-bridged and #sciml-bridged channels in the Julia Slack\nThe #diffeq-bridged and #sciml-bridged channels in the Julia Zulip\nOn the Julia Discourse forums\nSee also SciML Community page","title":"Contributing"},{"category":"section","location":"#Reproducibility","page":"Home","text":"<details><summary>The documentation of this SciML package was built using these direct dependencies,</summary>\n\nusing Pkg # hide\nPkg.status() # hide\n\n</details>\n\n<details><summary>and using this machine and Julia version.</summary>\n\nusing InteractiveUtils # hide\nversioninfo() # hide\n\n</details>\n\n<details><summary>A more complete overview of all dependencies and their versions is also provided.</summary>\n\nusing Pkg # hide\nPkg.status(; mode=PKGMODE_MANIFEST) # hide\n\n</details>\n\nusing TOML\nusing Markdown\nversion = TOML.parse(read(\"../../Project.toml\", String))[\"version\"]\nname = TOML.parse(read(\"../../Project.toml\", String))[\"name\"]\nlink_manifest = \"https://github.com/SciML/\" *\n                name *\n                \".jl/tree/gh-pages/v\" *\n                version *\n                \"/assets/Manifest.toml\"\nlink_project = \"https://github.com/SciML/\" *\n               name *\n               \".jl/tree/gh-pages/v\" *\n               version *\n               \"/assets/Project.toml\"\nMarkdown.parse(\"\"\"You can also download the\n[manifest]($link_manifest)\nfile and the\n[project]($link_project)\nfile.\n\"\"\")","title":"Reproducibility"}]
}
